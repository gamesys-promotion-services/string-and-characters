package com.example.demo.characters;


public class CharacterExercise {
    public static void main(String[] a) {
        char isLetter = 90;
        char isDigit = 97;
        char isWhiteSpace = 65;
        char isUpperCase = 57;
        char isLowerCase = 32;
        /**
         * Using ALL variables above
         * Print out the values using
         * the Character methods.
         * All output must be true and no
         * method should be repeated.
         * Sample below
         */
        System.out.println(Character.isLetter(isLetter));
    }
}
